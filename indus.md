## 当前剩余未完成项

- 完整 vue-i18n 中/英国际化
- Sentry 等外部错误监控接入
- 会话导入、PDF 导出、跨设备同步
- Monaco 代码编辑、运行、调试能力
- 实时协作、分享链接、权限体系
- 多用户 JWT/OAuth2.0 鉴权与角色权限

## 已完成项

- 本地模型管理、混合推理策略：已接入 Ollama 本地模型发现与状态刷新，设置面板支持云端/本地/自动混合推理模式、本地模型选择和本地失败回退云端开关；后端 `/api/local-models` 可检测 Ollama 版本、模型列表和运行状态，`/api/chat` 会按任务复杂度在本地模型与云端供应商之间自动路由。

- 图片以外的语音/音频/视频多模态：已支持音频、视频、PDF 等非图片附件上传；发送后按正常文件 chip 展示，点击可在 Workspace 预览区打开 PDF、播放音频/视频，并向模型发送文件类型、大小等摘要信息，避免二进制内容乱码进入消息正文。

| API Key明文存储/传输 | 1. 前端：用CryptoJS加密后存储到IndexedDB（替代localStorage）<br>2. 核心：**新增后端层**，前端不直连第三方AI接口，通过后端转发API Key（避免暴露） |
| 恶意文件/输入 | 1. 文件上传：限制类型（白名单）、大小（如≤10MB）、校验文件哈希<br>2. 用户输入：过滤特殊字符，限制单次输入长度 |
| 无权限控制 | 若后续支持多用户，需接入JWT/OAuth2.0鉴权，区分管理员/普通用户 |

### 3. 稳定性与容错（用户体验底线）

| 优化点         | 具体方案                                                                      |
| -------------- | ----------------------------------------------------------------------------- |
| 长列表性能问题 | 接入`vue-virtual-scroller`实现消息列表虚拟滚动，仅渲染可视区域DOM：<br>```vue |

<template>
  <RecycleScroller
    class="messages"
    :items="chat.visibleMessages"
    :item-size="100"
    key-field="id"
  >
    <template v-slot="{ item: message }">
      <MessageBubble :message="message" />
    </template>
  </RecycleScroller>
</template>
``` |
| 网络/接口容错 | 1. 新增请求重试机制（指数退避）：<br>```ts
async function requestWithRetry(fn: () => Promise<any>, retries = 3) {
  try {
    return await fn()
  } catch (err) {
    if (retries > 0) return requestWithRetry(fn, retries - 1)
    throw err
  }
}
```<br>2. 全局错误捕获（已完成 Vue errorHandler + Sentry 上报 + 后端兜底日志）<br>3. 流式响应中断后支持续传 |
| 本地存储可靠性 | 用`localForage`替代localStorage，迁移到IndexedDB：<br>- 支持更大存储容量<br>- 异步操作避免阻塞主线程<br>- 自动降级兼容低版本浏览器 |

### 4. 产品化与合规（上线必备）

| 改造点     | 具体动作                                              |
| ---------- | ----------------------------------------------------- |
| 响应式适配 | 补充移动端/平板样式，基于媒体查询优化布局：<br>```css |

@media (max-width: 768px) {
.sidebar {
position: fixed;
top: 0;
left: 0;
height: 100vh;
transform: translateX(-100%);
transition: transform 0.3s;
}
.sidebar.show {
transform: translateX(0);
}
}

```|
| 合规性文档 | 新增：<br>1. 隐私政策（明确数据仅本地存储/后端存储范围）<br>2. 用户协议（使用条款、免责声明）<br>3. Cookie政策（若使用） |
| 国际化/无障碍 | 1. 接入`vue-i18n`支持多语言（中/英）<br>2. 适配WCAG标准：语义化标签、键盘导航、屏幕阅读器支持 |
| 部署自动化 | 1. 用Docker打包项目，编写`Dockerfile`/`docker-compose.yml`<br>2. 配置GitHub Actions/GitLab CI，实现「提交→测试→构建→部署」自动化 |

## 二、高难度但高吸引力的功能点（差异化竞争力）
这些功能技术复杂度高，但能显著提升产品价值，拉开与同类工具的差距：

### 1. 多模态交互增强（难度★★★★☆）
- **现状**：仅支持文本/文件上传，无图片/语音交互；
- **实现方向**：
  - 图片理解：对接GPT-4V/文心一言视觉版，支持上传图片提问（如“分析这张代码截图的问题”）；
  - 语音交互：集成Web Speech API/阿里云TTS/ASR，实现“语音输入→文字”“AI回复→语音播报”；
  - 音频/视频解析：支持上传音频/短视频，提取文字并分析内容；
- **核心难点**：
  - 多模态数据处理（音频解码、图片压缩、分片上传）；
  - 不同AI供应商多模态接口适配（参数/格式差异大）；
- **吸引力**：从“纯文本聊天”升级为“全维度交互”，覆盖设计、教育、办公等场景。

### 2. 会话智能管理（难度★★★☆☆）
- **现状**：仅支持新建/删除会话，无高效管理能力；
- **实现方向**：
  - 会话分类/标签：支持打标签、分组，按标签筛选；
  - 全文检索：基于`lunr.js`实现聊天内容索引，支持按内容/时间/标签搜索；
  - 智能总结：调用AI接口自动生成长会话摘要（已完成当前会话摘要生成/更新、搜索命中、Markdown/JSON 导出保留与单元/E2E 覆盖；后续可继续扩展批量总结）；
  - 会话导出/导入：支持导出为Markdown/PDF/JSON，跨设备同步；
- **核心难点**：
  - 前端全文检索性能（大量会话时的索引构建/查询速度）；
  - 会话总结的Prompt工程（生成精准、简洁的摘要）；
- **吸引力**：解决用户“会话多了找不到”的痛点，提升效率。

### 3. 自定义Agent/提示词工程（难度★★★★☆）
- **现状**：无定制化Prompt能力；
- **实现方向**：
  - Prompt模板库：内置“代码重构”“文案创作”“面试辅导”等模板，支持用户自定义/分享；
  - 智能Agent：支持创建专属Agent（如“前端开发助手”），配置预设Prompt、模型、参数，一键调用；
  - 工作流编排：支持多步骤Prompt串联（如“需求分析→代码生成→注释优化”）；
- **核心难点**：
  - Prompt模板的可视化编辑（变量替换、拖拽编排）；
  - 不同AI模型的Prompt适配（格式/参数差异）；
- **吸引力**：从“单次聊天”升级为“场景化解决方案”，满足专业用户定制化需求。

### 4. 代码交互增强（难度★★★☆☆）
- **现状**：仅展示/复制代码，无执行能力；
- **实现方向**：
  - 代码在线运行：集成Monaco Editor/CodeSandbox，支持在聊天界面直接运行前端/后端代码；
  - 代码高亮/补全：基于Monaco增强代码块，支持语法高亮、智能补全、错误提示；
  - 代码调试：显示运行日志、报错信息，支持一键复制调试结果；
- **核心难点**：
  - 代码运行沙箱安全（防止恶意代码执行）；
  - 多语言运行时适配（JS/TS/Python/Java）；
- **吸引力**：精准命中程序员用户需求，从“看代码”到“用代码”，提升核心场景价值。

### 5. 实时协作聊天（难度★★★★★）
- **现状**：仅本地会话，无协作能力；
- **实现方向**：
  - 多人实时协作：支持邀请好友共同编辑会话，实时同步聊天内容；
  - 会话分享：生成临时链接，支持“只读/可编辑”权限；
  - 光标同步/批注：类似腾讯文档，在聊天内容上批注、讨论；
- **核心难点**：
  - WebSocket/Socket.IO状态同步（冲突解决）；
  - 权限精细化管理（不同角色操作限制）；
- **吸引力**：从“个人工具”升级为“团队协作工具”，打开B端市场。

### 6. 本地模型支持（难度★★★★★）
- **现状**：依赖第三方AI API，无本地运行能力；
- **实现方向**：
  - 对接Ollama/LLaMA.cpp，支持加载本地大模型（无网络也能用）；
  - 混合推理：简单任务用本地模型（隐私高），复杂任务自动切换云端API；
  - 模型管理：前端可视化管理本地模型（版本、显存、启动参数）；
- **核心难点**：
  - 前端与本地模型的IPC/HTTP通信；
  - 本地模型资源占用监控（避免前端卡顿）；
- **吸引力**：解决隐私顾虑（数据不上传），覆盖企业/医疗等敏感场景。

## 三、落地优先级建议
1. **第一阶段（上线MVP）**：完成「工程化改造+安全性加固+基础稳定性优化」，保证产品能稳定上线；
2. **第二阶段（核心体验）**：迭代「会话智能管理+多模态基础功能（图片）+代码交互增强」，提升核心用户体验；
3. **第三阶段（差异化）**：上线「自定义Agent+实时协作」，打造核心竞争力；
4. **第四阶段（高端场景）**：落地「本地模型支持+全模态交互」，覆盖小众高价值场景。

## 四、关键技术选型建议（降低开发成本）
| 功能场景 | 推荐开源库/工具 |
|----------|----------------|
| 虚拟滚动 | vue-virtual-scroller |
| 本地存储 | localForage（IndexedDB封装） |
| XSS防护 | DOMPurify |
| 全文检索 | lunr.js/elasticsearch（后端） |
| 多语言 | vue-i18n |
| 实时协作 | Socket.IO/Yjs |
| 代码编辑 | monaco-editor |
| 错误监控 | Sentry |
| 自动化部署 | GitHub Actions + Docker |

通过以上改造，项目将从“demo级”升级为“工业级”，既满足上线的稳定性/安全性要求，又能通过高价值功能形成差异化竞争力。
```
